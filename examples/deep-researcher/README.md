# Chatkit Demo 1

Vue 3 + TypeScript + Vite scaffolded with pnpm.

## Quickstart
- Install deps: `pnpm install`
- Dev server: `pnpm dev`
- Type-check and build: `pnpm build`
- Preview build: `pnpm preview`

## Stack
- Vite (rolldown-vite) + Vue 3
- TypeScript + vue-tsc
- pnpm
