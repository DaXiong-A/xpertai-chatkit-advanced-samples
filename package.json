{
  "name": "xpertai-chatkit-advanced-samples",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "concurrently --kill-others-on-fail --names backend,frontend \"pnpm -C examples/chatkit-host run serve\" \"pnpm -C examples/deep-researcher run backend\" \"pnpm -C examples/deep-researcher run frontend\"",
    "mindmap-assistant": "cd examples/mindmap-assistant && pnpm run dev"
  },
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
